use std::error::Error;
// todo process args (1 arg - github username)
// use reqwest to make the http request  
// use serde + serde_json to deserialize the response 

pub fn run() -> Result<(), Box<dyn Error>> {
    Ok(())
}